<%= form_for(@supplier) do |f| %>
  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% @supplier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "名称" %><br />
    <%= f.text_field :name ,:size => "60x12" %>
  </div>
  <div class="field">
    <%= f.label "商品" %><br />
    <%= f.text_area :goods ,:size => "60x12"%>
  </div>
  <div class="field">
    <%= f.label "联系方式" %><br />
    <%= f.text_area :contact, :size => "60x12"%>
  </div>
  <div class="field">
    <%= f.label "地址" %><br />
    <%= f.text_area :address ,:size => "60x12"%>
  </div>
  <div class="actions">
    <%= f.submit "提交" %>
  </div>
<% end %>
